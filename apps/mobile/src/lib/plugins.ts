/**
 * plugins.ts — Plugin Registration
 *
 * This is where you register plugins for this app instance.
 * Add plugins here to extend the app with new tabs and features.
 *
 * Example:
 *   import { eventsCalendarPlugin } from '@brew-loyalty/plugin-events'
 *   pluginRegistry.register(eventsCalendarPlugin)
 */

import { pluginRegistry } from './PluginRegistry'

// Register plugins here:
// pluginRegistry.register(eventsCalendarPlugin)

export { pluginRegistry }
